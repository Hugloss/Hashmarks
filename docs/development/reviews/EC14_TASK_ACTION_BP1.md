# EC-14 Task-Action BP1 Freeze

## Exact authority

Parent main: `56345f5a815d4e2d57a7ce632eba7d150fc801a4`.

Selected owner: `hashmarks/codemap/task_action_projection.py`.

Source blob identity: `8e9489649d8fb805603159f1c4462e26fbdfc0fd`.

Direct owning test: `tests/test_task_action_map.py`.

Direct-test blob identity: `fcf4dbd63833bf969cc3ad43f34d8709fb06f789`.

Current debt authority from `ruff-debt-baseline.json`: **189 excess / 4 functions / 16 rule findings**.

## Responsibility boundary

The selected owner projects canonical task evidence into bounded edit, verification, contract, inspect, related, ambiguity and structural-owner evidence. It must not rerank canonical retrieval, manufacture repository paths, execute verification, select repairs, or become workflow authority.

## Frozen behavior ring

The production edit phase must preserve the exact bytes of this BP1 direct owning test and run it unchanged. The broader semantic ring is:

- `tests/test_task_action_map.py`
- `tests/test_task_action_session_reuse.py`
- `tests/test_task_action_stale_workspace.py`
- `tests/test_structural_owner_resolution.py`
- `tests/test_verification_selection_identity.py`
- `tests/test_ambiguity_quality.py`
- `tests/test_b2_multi_owner_ambiguity.py`
- `tests/test_omitted_alnum_task_locality.py`

The repository CI remains the broader gate.

## Frozen invariants

- `find_task()` remains canonical retrieval authority and canonical rank/provenance are preserved.
- decision-session cache identity and deep-copy isolation are unchanged.
- explicit test/build/config surface selection remains deterministic.
- structural ownership and exact-identifier displacement remain fail-closed under ambiguity.
- verification selection remains identity-aware and does not convert unresolved qualified identity into confidence.
- archive/live-owner discrimination remains fail-closed.
- stale repository evidence is not reused as current authority.
- public ordering, bounds and schema remain unchanged.

## Locality gate

Before claiming BP2 closure, obtain complete pre/post structural-locality evidence for every introduced helper/stage. A call/reference bound exhaustion is incomplete evidence: raise the supported bound and rerun. Observed caller count is review evidence only.

## Edit admission

Only internal responsibility decomposition is admitted. No new module owner, public surface, cache, persistence path, ranking authority or compatibility path is permitted.

The exact four Ruff-bearing functions still must be read from executable Ruff inventory during qualification; the baseline aggregate is not sufficient to invent per-function diagnostics. If the local runner cannot execute that inventory, GitHub CI/qualification evidence must supply it before debt-reduction claims are closed.
