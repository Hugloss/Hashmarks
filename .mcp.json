{
  "mcpServers": {
    "hashmarks": {
      "command": "uv",
      "args": [
        "run",
        "--no-sync",
        "hashmarks",
        "--workspace",
        ".",
        "mcp"
      ]
    }
  }
}
