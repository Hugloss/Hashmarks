{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "hashmarks": {
      "type": "local",
      "command": [
        "uv",
        "run",
        "--frozen",
        "--no-sync",
        "hashmarks",
        "--workspace",
        ".",
        "mcp"
      ],
      "cwd": ".",
      "enabled": true
    }
  }
}
